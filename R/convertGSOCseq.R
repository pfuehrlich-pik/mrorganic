#' Convert GSOCseq
#'
#' Convert GSOCseq data
#'
#'
#' @param x data
#' @return data
#' @author Jan Philipp Dietrich

convertGSOCseq <- function(x) {
  stop("work in progress")
  #ra <- terra::project(r, res=0.25, y="+proj=longlat +datum=WGS84 +no_defs") ?
  return(NULL)
}
